specVersion: "v1beta"
serviceId: "vscodestuff"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vscodestuff-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
